// Optional overrides. Keys must match actual slugs in src/content/projects.
export const categoryBySlug = {
  // 'your-graphic-project-slug': 'graphic',
};
// Missing values are intentionally empty; never invent personal contact details.
export const profile = { resumeUrl: '', email: '', socials: [] };
